package com.example.p2p.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Principal ───────────────────────────────────────────────────────────
val Primary      = Color(0xFFF59E0B)   // Dorado — divisa / oro
val PrimaryLight = Color(0xFFFBBF24)   // Dorado claro
val PrimaryDark  = Color(0xFFD97706)   // Dorado oscuro
val PrimaryMint  = Color(0xFF10B981)   // Esmeralda — éxito

// ── Fondos ────────────────────────────────────────────────────────────────────
val BackgroundApp     = Color(0xFF0A0F1E)   // Azul marino profundo
val SurfaceColor      = Color(0xFF111827)   // Superficie principal
val SurfaceElevated   = Color(0xFF1C2333)   // Superficie elevada
val DarkSurface       = Color(0xFF0D1117)   // Fondo más oscuro
val DarkSurface2      = Color(0xFF161D2E)   // Secundario oscuro

// ── Estado ────────────────────────────────────────────────────────────────────
val SuccessColor = Color(0xFF10B981)   // Esmeralda
val DangerColor  = Color(0xFFEF4444)   // Rojo
val WarningColor = Color(0xFFF59E0B)   // Ámbar dorado
val InfoColor    = Color(0xFF3B82F6)   // Azul

// ── Texto ─────────────────────────────────────────────────────────────────────
val TextMain    = Color(0xFFF8FAFC)   // Blanco suave
val TextSecond  = Color(0xFFCBD5E1)   // Gris claro
val TextMuted   = Color(0xFF94A3B8)   // Gris medio
val TextSubtle  = Color(0xFF475569)   // Gris oscuro

// ── Bordes ────────────────────────────────────────────────────────────────────
val BorderColor  = Color(0xFF1E2D3D)   // Borde sutil oscuro
val DividerColor = Color(0xFF1A2540)   // Divisor

// ── Overlay ───────────────────────────────────────────────────────────────────
val OverlayLight = Color(0x14FFFFFF)
val OverlayDark  = Color(0x80000000)

// ── Bancos peruanos ───────────────────────────────────────────────────────────
val BcpColor       = Color(0xFFE85D04)
val InterbankColor = Color(0xFF00873A)
val BbvaColor      = Color(0xFF004481)
val YapeColor      = Color(0xFF6B21A8)
val PlinColor      = Color(0xFF0891B2)